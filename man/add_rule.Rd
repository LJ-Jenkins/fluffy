% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/abb_add_rule_generics.r
\name{add_rule}
\alias{add_rule}
\alias{add_cross_rule}
\alias{add_type_rule}
\alias{add_coerce_rule}
\title{Add Rules to a Registry}
\usage{
add_rule(
  obj,
  name,
  validator_fn,
  schema_fn = NULL,
  rule_type = c("validate", "control", "transform", "finalize")
)

add_cross_rule(obj, name, rule_names, cross_fn)

add_type_rule(obj, type_name, type_fn)

add_coerce_rule(obj, coerce_name, coerce_fn)
}
\arguments{
\item{obj}{\code{Registry}, \code{Schema}, or \code{Validator} object.}

\item{name}{String specifying the name of the rule to add.
Rule names cannot be the same as any existing rule names.}

\item{validator_fn}{Function that validates a data field against a schema field.
Every validator rule function must return \code{NULL} or a named list. A
\code{NULL} return indicates validation success with no transformed data or
alteration to the control flow. A named list return can accept the
following elements:
\itemize{
\item \code{data}: the (optionally transformed) data value to be reassigned and
passed to subsequent rules. If not returned, the original data value
is used.
\item \code{error}: a string error message if validation fails. If not returned,
validation is assumed to have succeeded.
\item \code{continue}: boolean indicating whether to continue validating subsequent
rules. If not returned, assumed to be \code{TRUE}.
}}

\item{schema_fn}{Function to validate a schema field definition. If \code{NULL}, a
default function that performs no validation is used. \code{Schema}
validation functions should return \code{NULL} if the schema field is
valid, and a string error message if invalid.}

\item{rule_type}{String specifying the type of rule to add. Must be one of
\code{"control"}, \code{"transform"}, \code{"validate"}, \code{"finalize"}.}

\item{rule_names}{Character vector specifying the names of the existing schema
rules that the cross rule will operate over. Must be of
length 2 or more, and all names must be of existing schema rules
in the \code{Registry}.}

\item{cross_fn}{Function to validate schema field definitions across multiple
schema rules. \code{Schema} cross functions should return \code{NULL} if the
schema fields are valid, and a string error message if invalid.}

\item{type_name, coerce_name}{String specifying the name of the type/coerce rule value to add.}

\item{type_fn, coerce_fn}{Function to validate/coerce a field value.}
}
\value{
Invisibly returns the input object with the new rule added to the
associated \code{Registry}.
}
\description{
Add validator, schema, (schema) cross, type, or coerce rules to a
\link{Registry}, \link{Schema}, or \link{Validator} object.
}
\details{
To be able to correctly pass the required arguments for the
data/schema validations, rule functions must have the following
argument semantics:

Note: \code{field} and \code{schema_field} are positional, and thus can be
called anything, whilst \code{.self} (the object itself: \code{Schema} for
schema rules, \code{Validator} for validator rules) and \code{.data} (the
full data in the walk) are named arguments and must be
named as such.

type/coerce rule functions:
\itemize{
\item \verb{function(field)}
}

schema/cross rule functions:
\itemize{
\item \verb{function(field, ...)}
\item \verb{function(field, .self, ...)} |
\verb{function(field, .schema, ...)}
\item \verb{function(field, .self, .schema)}
}

validator rule functions:
\itemize{
\item \verb{function(field, schema_field, ...)}
\item \verb{function(field, schema_field, .self, ...)} |
\verb{function(field, schema_field, .data, ...)}
\item \verb{function(field, schema_field, .self, .data)}
}

As \link{Registry} uses environments to store rules, which are
mutable, \link{add_rule} methods copy the existing environment
and the new rule into a new environment, meaning that the
original object is not modified. See examples.

For full details see the vignettes on
\href{../doc/custom-rules.html}{adding custom rules}.

For more information on validating data with a \code{Schema}, see the
vignettes on
\href{../doc/validation-rules.html}{builtin rules} and
\href{../doc/validating-data.html}{data validation}.
}
\examples{
mySchema <- Schema(list(check_my_attr = 1L))
mySchema@errors # doesn't recognise rule

mySchema <- add_rule(
  obj = mySchema,
  name = "check_my_attr",
  validator_fn = function(data_field, schema_field, ...) {
    if (attr(data_field, "my_attr") != schema_field) {
      list(error = "Data doesn't match schema 'my_attr'.")
    }
  },
  schema_fn = function(schema_field, ...) {
    if (!is.character(schema_field) || length(schema_field) != 1L) {
      "Must be length 1 character"
    }
  },
  rule_type = "validate"
)

# rule recognised and schema automatically re-validated
mySchema@errors

# validation works with the new rule
mySchema@schema$check_my_attr <- "Hi"
Validator(structure(1L, my_attr = "Hi"), mySchema)@valid # TRUE

# schema cross rules invalidate when constituent rules are invalid
mySchema <- add_cross_rule(
  obj = mySchema,
  name = "min_and_max_val_add_to_10",
  rule_names = c("min_val", "max_val"),
  cross_fn = function(schema_field, ...) {
    if (schema_field$min_val + schema_field$max_val == 10) {
      "min_val and max_val cannot add to 10."
    }
  }
)
mySchema@schema <- list(min_val = 2, max_val = 8)
mySchema@errors # cross rule error

r <- Registry()
r <- add_type_rule(r, "my_type", function(x) {
  inherits(x, "my_type")
})
s <- Schema(list(type = "my_type"), registry = r)
s@valid # TRUE
Validator(structure(1, class = "my_type"), s)@valid # TRUE

r <- add_coerce_rule(r, "my_type", function(x) {
  structure(x, class = c("my_type", class(x)))
})
s <- Schema(list(coerce = "my_type"), registry = r)
v <- Validator(1, s)
class(v@data) # "my_type" "numeric"

# environments are copied, so original registry is not modified
r <- Registry()
r2 <- add_type_rule(r, "my_type", function(x) x)
identical(r@type_map, r2@type_map) # FALSE
}
\seealso{
\link{Registry}, \link{Schema}, and \link{Validator} classes.
}
